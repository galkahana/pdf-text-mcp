apiVersion: v1
kind: Service
metadata:
  name: pdf-text-mcp-server
  namespace: pdf-text-mcp
  labels:
    app: pdf-text-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: pdf-text-mcp-server
